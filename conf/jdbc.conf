{
	"jdbc": {
		"driver": "com.mysql.cj.jdbc.Driver",
		// default mysql port is 3306, mysql database name is texera, make sure to add ?serverTimezone=UTC to avoid issue
		"url": "jdbc:mysql://localhost:3306/texera?serverTimezone=UTC",
		// put your database username and password here
		"username": "",
		"password": ""
	}
}